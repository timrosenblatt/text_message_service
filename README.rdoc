Going to use memcache as the hotcache, DB for cold. Expiration will be
straightforward.

install with `brew install memcached` or whatever you prefer

The app assumes memcache is already running

Any need for DB transactions?

Add a task for warming a cold cache from DB

Switch to pgsql